import 'package:flutter/material.dart';

class FilterCoinScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Filtro de Moedas'),
      ),
      body: Center(
        child: Text('Tela de Filtro de Moedas'),
      ),
    );
  }
}
